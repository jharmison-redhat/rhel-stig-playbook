---
ansible-navigator:
  ansible:
    inventories: ~/.ansible/rhel8_hosts
  execution-environment:
    container-engine: podman
    enabled: True
    image: stig-ee:latest
    pull-policy: missing
  logging:
    level: info
  playbook-artifact:
    enable: False
